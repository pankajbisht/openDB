import {
  createNamespace,
  get,
  getCurrentNamespace,
  switchNamespace,
  setSeparator,
  getSeparator,
  generateKey,
} from './config.js';

import { version } from './version.js';

export default {
  version,
  createNamespace,
  getCurrentNamespace,
  switchNamespace,
  get,
  setSeparator,
  getSeparator,
  generateKey,
};
